# dependencies
node_modules/

# build output
_site/
_site*/

# The compatibility quiz answer key: every one of the 1,088 answer combinations
# and the outcome it reaches, built by scripts/assess-matrix.mjs.
#
# Generated, never committed, and that is a decision rather than an oversight.
# A Decap publish writes exactly one file, src/_data/assess.yml, and knows
# nothing about anything derived from it. So a checked-in copy would silently
# disagree with its own source the first time anyone edited the quiz through the
# CMS — not a merge conflict, which would at least be loud, but a file that
# still opens, still shows 1,088 rows, and is quietly wrong.
#
# Until 19 Aug this was excluded by intent alone: nobody had committed it
# because nobody had been told to. That works right up until someone clones the
# repo, runs the generator and commits the output because nothing stopped them.
#
# Anyone who needs it without running Node downloads it from the Artifacts
# section of any Health check run, as compatibility-quiz-answer-key.
/docs/assess-matrix.html

# Local scratch. Anything at the repo root starting with an underscore is a
# throwaway: preview builds, staging copies, tarballs, logs.
#
# The leading slash matters. Without it this would also swallow src/_data and
# src/_includes, which are real source.
#
# This used to be three narrower patterns, /_*/ and /_*.tgz and /_*.zip, which
# caught scratch directories and tarballs but not scratch files. Sixty-four of
# them had accumulated at the root by 8 Aug: build logs, a dumped stylesheet,
# marker files. None were tracked, but they made the folder hard to read.
/_*

# Per-machine tool settings. settings.local.json records which commands this
# machine has approved; it is personal to whoever is sitting at it and would
# only cause conflicts if it were shared.
.claude/

# misc
dist/
dist*/
build/
.DS_Store
Thumbs.db

# Orphaned page data and artwork. /mission/, /quality-standards/, /services/ and
# /technology/ are redirect stubs now, and no template reads any of these files.
# They were removed on 6 Aug, then a `git add -A src` quietly put them back on
# 7 Aug, which is what these lines are here to stop. Kept on disk in case a
# Technology page returns; kept out of the repo so 5.6MB of unreferenced images
# is not shipped on every deploy. They remain in git history either way.
/src/_data/mission.yml
/src/_data/quality_standards.yml
/src/_data/services.yml
/src/_data/technology.yml
# Named individually rather than as /src/assets/images/*.webp, which is what
# this was until 11 Aug. The wildcard did the job it was written for and then
# quietly caught the new homepage hero background as well. That would have
# deployed a site with no hero image and no error anywhere to explain it: the
# file is on the machine it was built on, so it looks right locally and is
# missing everywhere else. Three named files cannot do that to the next one.
/src/assets/images/method-qpcr.webp
/src/assets/images/method-dpcr.webp
/src/assets/images/method-sequencing.webp
# The 3.6MB original of the hero background. ingredients-lab.webp is the same
# picture at 1920px and 112KB, which is what the site loads. Kept on disk in
# case it needs re-cropping, kept out of the repo because nothing reads it.
/src/assets/images/Ingredients Lab.webp

# Stale git lock files parked here by the Cowork device bridge, which cannot
# unlink. Safe to delete the folder.
_gitlocks/

# Files the Cowork device bridge could not unlink, parked for manual deletion.
# Safe to delete the whole folder.
_todelete/
